var usage = require('node-usage');
 
module.exports; {
var pid = process.pid // you can use any valid PID instead
usage.lookup(pid, function(err, result) {
    var resultVar = result
    const resultOut = resultVar
    console.log(resultOut)
});
}
